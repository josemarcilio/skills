# Thread <thread-id>

<!-- One file per PR thread. Append a new entry each time the thread is worked; never rewrite
     old entries. "Last seen" is how the next run spots new replies. -->

- Thread: [<thread-id>](<pr-url>) · `<file>:<line>`
- Last seen comment: <comment id>
- Status: Answered | Fixed | Resolved | Needs user
  <!-- Answered: replied, thread open. Fixed: fix pushed and replied, not resolved.
       Resolved: concern addressed and thread resolved. Needs user: waiting on the user's decision;
       nothing posted yet. -->

## <yyyy-mm-dd> — entry <n>

- Asked: <one line>
- Found: <one line — what the code actually does>
- Decision: fix | reply only | ask user — <why, one line>
- Fix: `<sha>` <message> | —
- Reply: "<the exact reply posted>" (comment <comment_id>)
- Resolved: yes | no
