# Learnings — <item-id>

<!-- Harvest record. One entry per harvest; never rewrite earlier entries. The follow-up PR, not
     this file, is the durable result — this file is deleted with the other handoffs. -->

- Follow-up branch: `<branch>` · worktree `<user-home>/.../worktrees/<learnings-slug>`
- Follow-up PR: [<pr-id>](<url>) | not created
- Harvested ids: <L1, L2, ...>
- Processed threads: <thread-id, ...>

## <yyyy-mm-dd> — harvest <n> (trigger: approval | pre-complete | request)

- Covers approval of: <approvers, time> | —
- Proposed: <n> · Approved: <ids> · Rejected: <ids> · Edited: <ids>
- Conflicts decided: <id — choice> | —
- Commit: `<sha>` on the follow-up branch | nothing approved
